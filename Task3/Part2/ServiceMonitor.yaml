apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: novamarket-scale
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: novamarket-scale
  endpoints:
    - port: http
      path: /metrics
      interval: 5s