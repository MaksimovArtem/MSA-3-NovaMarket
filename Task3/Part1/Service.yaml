apiVersion: v1
kind: Service
metadata:
  name: novamarket-scale
  labels:
    app: novamarket-scale
spec:
  selector:
    app: novamarket-scale
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: NodePort